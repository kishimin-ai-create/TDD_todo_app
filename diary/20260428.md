# 2026-04-28 作業日誌

## 概要

フロントエンドの API 通信基盤とテスト環境を整備した。具体的には、OpenAPI 定義書から型・TanStack Query フック・MSW モックハンドラーを自動生成する orval の設定、および MSW v2 を Vitest に統合してコンポーネントテストで API をモックできる環境の構築を行った。

---

## 1. orval セットアップ（OpenAPI → 型 / TanStack Query / MSW 自動生成）

**コミット**: `0e32375` `feat: configure orval for type/mock/tanstack-query generation from OpenAPI spec`

### 背景

バックエンドはすでに hono-openapi で OpenAPI 定義書（`docs/spec/backend/openapi-generated-pretty.json`）を自動生成していた。フロントエンドでこれを手書きで追いかけるのは型の乖離や二重管理を生むため、定義書を単一の信頼できる情報源として、そこからフロントエンドに必要なコードを自動生成する方針を取った。

### 作業内容

- **`frontend/orval.config.ts` を新規作成**
  - `input.target` に `docs/spec/backend/openapi-generated-pretty.json` を指定
  - `output.client: 'react-query'` で TanStack Query フックを生成
  - `output.httpClient: 'fetch'` で axios 依存なしの fetch ベースに
  - `output.mock: { type: 'msw' }` で MSW ハンドラーを同時生成
  - `output.mode: 'split'` でフックは `index.ts` 一本、型定義は `models/` 以下にファイル分割
  - `override.mutator` に `src/api/client.ts#customFetch` を指定し、ベース URL 管理とエラーハンドリングを集約

- **`frontend/src/api/client.ts` を新規作成**
  - `VITE_API_BASE_URL` 環境変数でベース URL を差し込む fetch ラッパー
  - `response.ok` が false のとき JSON ボディをそのまま `throw` する

- **`frontend/package.json` にスクリプトと依存を追加**
  - `"generate": "orval"` スクリプト追加
  - `@faker-js/faker` を devDependency として追加（orval MSW 出力が内部で使用するが peerDeps に記載がなく実行時エラーになるため）

- **`frontend/vite.config.ts` に dev proxy を追加**
  - `/api` → `http://localhost:3000`（開発時の CORS 回避）

- **ESLint / TypeScript 設定の整備**
  - `eslint.config.js` の `globalIgnores` に `src/api/generated/**` を追加（生成コードへの lint 適用を除外）
  - `tsconfig.node.json` の `include` に `orval.config.ts` を追加（ESLint project service のスコープ外エラーを解消）

### 生成物

`npm run generate` 実行後に以下が生成された。

| ファイル | 内容 |
|---------|------|
| `src/api/generated/index.ts` | 全エンドポイントの TanStack Query フック（1,147行） |
| `src/api/generated/index.msw.ts` | 全エンドポイントの MSW ハンドラー + `getTDDTodoAppAPIMock()` アグリゲート（185行） |
| `src/api/generated/models/` | リクエスト・レスポンス型 70+ ファイル |

---

## 2. MSW v2 と Vitest の統合（コンポーネントテスト環境の整備）

**コミット**: `ec19bfe` `feat: integrate MSW v2 with Vitest for component testing`

### 背景

orval で MSW ハンドラーを生成できるようになったが、それをテストで実際に使うには Vitest のライフサイクルに MSW サーバーを組み込む必要があった。また既存のテスト環境には jsdom が入っておらず、`@testing-library/react` もインストールされていなかった。

### 作業内容

- **`frontend/src/test/server.ts` を新規作成**
  - `msw/node` の `setupServer` に `getTDDTodoAppAPIMock()` の全ハンドラーをスプレッドして渡す
  - 全テストで共有するシングルトンインスタンスとして export

- **`frontend/src/test/setup.ts` を新規作成**
  - `beforeAll`: `server.listen({ onUnhandledRequest: 'warn' })` — テスト開始前に MSW 起動
  - `afterEach`: `server.resetHandlers()` — `server.use()` で追加したハンドラーを各テスト後にリセット
  - `afterAll`: `server.close()` — 全テスト終了後に MSW 停止
  - `import '@testing-library/jest-dom/vitest'` でカスタムマッチャーを登録（v6 系は `/vitest` エントリポイントを使う必要がある）

- **`frontend/vite.config.ts` を更新**
  - `test.environment: 'jsdom'` — Node.js 環境ではなく jsdom でテストを実行
  - `test.setupFiles: ['src/test/setup.ts']` — 全テストファイルの前にセットアップを自動実行

- **devDependency 追加**
  - `@testing-library/react`（コンポーネントの `render` に必要）
  - `jsdom`（Vitest の jsdom 環境として必要）

### 確認

既存の `App.test.tsx` を実行して 1 件パスを確認。typecheck・lint ともに通過。

---

## 3. ブログ記事執筆

**コミット**: `83bd4de` `docs: add articles for orval setup and MSW/Vitest integration`

今日の作業を技術記事として `blog/` に書き残した。

- `blog/orvalでOpenAPI定義書から型・モック・TanStackQueryフックを自動生成する.md`
  - orval.config.ts の各オプション解説、カスタム mutator の仕組み、生成物の説明、つまずきポイント

- `blog/MSW-v2とVitestを統合してコンポーネントテストを整備する.md`
  - MSW v2 の `setupServer` を Vitest ライフサイクルに組み込む方法、per-test オーバーライドパターン

---

## 残課題

- フロントエンドのコンポーネント実装はまだ未着手。`src/api/generated/index.ts` の TanStack Query フックを実際のコンポーネントで使い始める段階ではない。
- MSW ハンドラーのオーバーライドを使った実際のコンポーネントテストはまだ書かれていない（環境は整備済み）。
- `VITE_API_BASE_URL` を使った本番向けビルド設定は未検討。

---

## 変更ファイルサマリー

| ファイル | 種別 |
|---------|------|
| `frontend/orval.config.ts` | 新規 |
| `frontend/src/api/client.ts` | 新規 |
| `frontend/src/api/generated/index.ts` | 生成（手動編集不可） |
| `frontend/src/api/generated/index.msw.ts` | 生成（手動編集不可） |
| `frontend/src/api/generated/models/` | 生成（手動編集不可） |
| `frontend/src/test/server.ts` | 新規 |
| `frontend/src/test/setup.ts` | 新規 |
| `frontend/vite.config.ts` | 更新（proxy + test config） |
| `frontend/package.json` | 更新（generate スクリプト + devDeps） |
| `frontend/eslint.config.js` | 更新（globalIgnores） |
| `frontend/tsconfig.node.json` | 更新（include） |
| `blog/orvalでOpenAPI定義書から...md` | 新規 |
| `blog/MSW-v2とVitestを統合して...md` | 新規 |
