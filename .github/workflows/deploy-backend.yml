name: Deploy backend to Cloudflare Workers

on:
  push:
    branches: [main]
    paths:
      - 'backend/**'
      - '.github/workflows/**'
  workflow_dispatch:

permissions:
  contents: read

# TODO: This workflow is intentionally a placeholder.
# The backend currently depends on @hono/node-server and mysql2 TCP access,
# which are not compatible with the current Cloudflare Workers target.
# After migrating the data layer to a Workers-compatible adapter,
# replace this job with cloudflare/wrangler-action@v3 using
# CLOUDFLARE_API_TOKEN and CLOUDFLARE_ACCOUNT_ID.

jobs:
  todo:
    runs-on: ubuntu-latest

    steps:
      - name: Explain current blocker
        run: |
          echo "TODO: Cloudflare Workers deployment is not enabled yet."
          echo "The backend currently uses @hono/node-server plus mysql2-based TCP database access."
          echo "Migrate the runtime and data access to Workers-compatible components before enabling wrangler deploy."
